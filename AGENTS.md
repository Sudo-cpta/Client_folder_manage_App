# AGENTS.md

このリポジトリは、**日本の税制・社会保険を計算する公開ツール**です。
出力が金銭的な意思決定に使われ、税理士事務所の名義で公開されるため、
通常のWebアプリより計算の正確性に対する要求が高くなっています。

## 構成

| ファイル | 役割 |
|---|---|
| `index.html` | 本体。単一ファイル・サーバー不要・外部通信なし |
| `test-engine.mjs` | `index.html` から計算エンジンを直接抽出して検証する |
| `README.md` | 対応制度、パラメータの所在、既知の近似 |

制度パラメータは `index.html` の `<script>` 冒頭に集約されています
（`BASIS_DATE` から `TUITION_CAP` まで）。ロジックを直す前に、まずここを疑ってください。

テストは `node test-engine.mjs` で実行します。**`index.html` を編集したら必ず実行してください。**
テストは出荷するコードそのものを読むので、実装とテストが乖離することはありません。

## Code Review Rules

### 最優先：制度パラメータの数値

**記憶で判断せず、必ず一次資料に当たってください。** この分野は毎年改正され、
学習データの内容は高い確率で古くなっています。参照すべきは以下です。

- 所得税・給与所得控除・各種控除 → 国税庁、財務省「税制改正の大綱」
- 個人住民税・調整控除・非課税限度額 → 総務省、各自治体
- 社会保険・適用拡大・保険料率 → 厚生労働省、日本年金機構、全国健康保険協会
- 国民健康保険 → 各市区町村（**全国標準値は存在しません**）
- 修学支援新制度 → 文部科学省、日本学生支援機構

適用年分と適用年度を取り違えていないかを必ず確認してください。
**住民税は前年所得課税**です（令和8年分の所得 → 令和9年度の住民税）。

### 過去に実際に起きた誤りの類型

同種の誤りが他の箇所にないかを重点的に見てください。

1. **法定の定額を、実額の差で計算していた**
   調整控除の人的控除差を「所得税の控除額 − 住民税の控除額」で算出していました。
   人的控除差は地方税法上の定額であり、実差ではありません。
   特定親族特別控除・配偶者特別控除の差額は **0円** です。
   → 現在は `JINTEKI_SA` に独立したマスタとして分離しています。
   同様に「2つの制度の値を引き算して第3の値を導いている」箇所は
   すべて疑ってください。

2. **改正の反映が1年古い**
   令和7年度改正だけを見て令和8年度改正を見落とし、非課税ラインを
   160万円（正しくは178万円）としていました。

3. **構成要素の欠落**
   国民健康保険に平等割と軽減（7割／5割／2割）が入っておらず、
   対象層の保険料を過大に計上していました。

4. **境界値の1,000円ずれ**
   「◯◯万円の壁」は通常「その金額までは大丈夫」を意味します。
   `incomeForShotoku()` は「基準を満たす最大の給与収入」を返します。
   一方 130万円・106万円の壁は「その金額以上で該当」です。
   両者の意味の違いを保ったまま扱えているか確認してください。

### 確認してほしい観点

- 合計所得と給与収入の換算が、その年分の給与所得控除と整合しているか
- 控除の段階表の**上限・下限の境界**（`<=` と `<` の取り違え）
- 年分を切り替えたときに、その年分に存在しない壁が出ていないか／
  存在する壁が消えていないか
- 扶養者側（親・配偶者）の税額計算で、本人の所得の変化が正しく伝播しているか
- 概算・近似で処理している箇所が、画面上でも概算と明示されているか

### 指摘の粒度

- **数値の誤りは根拠となる一次資料のURLを添えて**指摘してください
- 表示上の丸め誤差（数百円程度）より、**制度の解釈の誤り**を優先してください
- 「概算です」と明示済みの箇所を精密でないと指摘するのは不要です。
  ただし**概算であることが利用者に伝わっていない**場合は指摘してください
- 近似が妥当かどうか判断に迷う場合は、影響額を試算して添えてください

## 変更を加える場合

- `index.html` は単一ファイルです。**複数のエージェントが同時に編集すると
  ほぼ確実にコンフリクトします。** 書き込むのは常に1人にしてください
- 制度パラメータを変更したら `test-engine.mjs` にアサーションを追加してください
- 制度の解釈が変わる修正をしたら、`VERSION` と `CHANGELOG` を更新してください
- 画面に表示している説明文が、修正後のロジックと矛盾していないか確認してください
  （控除額は維持されるが調整控除は失われる、といったケースで実際に矛盾が起きました）
